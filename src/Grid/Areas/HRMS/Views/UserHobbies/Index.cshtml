@model IEnumerable<Grid.Features.HRMS.Entities.UserHobby>

<h2>Manage UserHobby</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hobby.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.EmployeeCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hobby.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.EmployeeCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
