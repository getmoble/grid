@model Grid.Features.CRM.Entities.CRMLeadStatus

<div class="btn-group pull-right" role="group">
    @Html.ActionLink("Edit", "Edit", "CRMLeadStatus", new { id = Model.Id }, new { @class = "btn btn-primary" })
    @Html.ActionLink("Delete", "Delete", "CRMLeadStatus", new { id = Model.Id }, new { @class = "btn btn-primary" })
</div>

<h2>@Model.Name</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CreatedOn)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.CreatedOn)
        </dd>

    </dl>
</div>
