@model Grid.Features.CRM.Entities.CRMSalesStage

<div class="btn-group pull-right" role="group">
    @Html.ActionLink("Edit", "Edit", "CRMSalesStages", new { id = Model.Id }, new { @class = "btn btn-primary" })
    @Html.ActionLink("Delete", "Delete", "CRMSalesStages", new { id = Model.Id }, new { @class = "btn btn-primary" })
</div>

<h2>@Model.Name</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Status)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Status)
        </dd>
    </dl>
</div>
